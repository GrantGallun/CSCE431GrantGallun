<% content_for :title, @user.username %>

<h1>User</h1>

<%= render @user %>

<p>
  <%= link_to "Edit", edit_user_path(@user) %> |
  <%= link_to "Back", users_path %>
</p>

<%= button_to "Destroy", @user, method: :delete, class: "danger" %>
