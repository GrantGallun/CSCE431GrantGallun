# Date display formats used in views, e.g. date.to_fs(:published) => "August 1, 2008".
# The built-in :long format zero-pads the day ("August 01, 2008").
Date::DATE_FORMATS[:published] = "%B %-d, %Y"
